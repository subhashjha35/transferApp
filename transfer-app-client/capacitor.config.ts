import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'transfer-app-client',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
